import ui.UserInterface;

/* This class is the entry point. It should be very simple and
   only responsible for starting the application. */

public class Main {
    public static void main(String[] args) {
        UserInterface ui = new UserInterface();
        ui.startApp();
    }
}